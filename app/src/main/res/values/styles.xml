<resources>

    <style name="SearchView_Photos" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/text_search_view_hint</item>
        <item name="android:textSize">@dimen/search_view_text_size</item>
        <item name="android:textColor">@color/text_search_view_text</item>
    </style>

</resources>
